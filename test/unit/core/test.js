describe('Initializing', function() {
  it('test', function() {
    expect(true).to.equal(true);
  });
  // it('with new', function() {
  //   expect(new Moon() instanceof Moon).to.equal(true);
  // });
});
